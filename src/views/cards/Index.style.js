import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  cards: {
    alignItems: 'center',
    // position: 'absolute',
    // bottom: -105,
    // overflow: 'scroll',

    // borderWidth: 1,
    // borderColor: 'black',
    // borderStyle: 'solid',
  },
  card: {

  }
});
export default styles